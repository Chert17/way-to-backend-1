export type LoginInputModel = {
  loginOrEmail: string;
  password: string;
};

export type MeViewMOdel = {
  email: string;
  login: string;
  userId: string;
};

export type RegisterInputModel = {
  login: string;
  email: string;
  password: string;
};
